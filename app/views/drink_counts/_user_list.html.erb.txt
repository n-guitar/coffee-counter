

<table class="table table-hover table-striped">
  <thead>
  <tr class="row">
    <td>名前<td>
    <td>回数<td>
    <td>飲み物<td>
  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
  <tr class="row">
    <td><%= user.name %></td>
    <td><%= @drinkcounts.where(:user_id => user.id).count %></td>
    <% @drinks.each do |drink| %>
      <td><%= link_to drink.menu, drink_counts_path(:user_id => user.id, :id => drink.id ) ,:method => :post, class: "btn btn-info"%></td>
    <% end %>
  </tr>
  <% end %>
  </tbody>
</table>  